#pragma once

#include "compatibility/profile.h"

// Transitional aliases; reviewed data and matching rules live in Compatibility.
using TrampolinePolicy = kqpet::compatibility::TrampolinePolicy;
using TargetEndpointProfile = kqpet::compatibility::EndpointProfile;
using TargetProfile = kqpet::compatibility::Profile;
