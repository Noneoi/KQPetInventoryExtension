# 发行说明

## 2.0.0-preview.2

预览构建 `2.0.0-7777a645baa3-20260917T142005Z`，完整 Release 回归 72 项通过。

- 详情、战力、培养和商店按官方时代开关养成系统：没有的系统不显示也不计算。
- 潜能、源兽装备、传说石显示官方名称与等级效果。
- 精灵详细的战斗力只保留官方当前/极限、持有可达/至高和培养状态；分项在精灵分析里查看。
- 星轮突破仍仅适用于灵初。

安装请下载本发行页的 **copy-ready** ZIP。

## 2.0.0（源码）

本版本将精灵、商店、日常和资产功能整合为工作台，并完成本地缓存、手动公共资料更新及培养分析的主要改造。版本号来自 CMake 项目配置；具体二进制还包含独立的 `releaseId`、源码摘要和工具链信息。

### 复制即用安装与升级

推荐从 [v2.0.0-preview.2 发行页](https://github.com/Noneoi/KQPetInventoryExtension/releases/tag/v2.0.0-preview.2) 下载名称含 **copy-ready** 的 ZIP。关闭氪奇、解压，将 **启动精灵工作台.cmd** 和 **KQPetQuickStart** 文件夹复制到 `KQPro*.exe` 同目录，再双击 **启动精灵工作台.cmd**。

程序自动安装或更新扩展并启动，无需输入路径或命令，原版客户端和已有缓存保留。复制即用包沿用已验证标准包的应用二进制与报告，应用版本不变。

高级 PowerShell 部署仍可使用。开发者可通过 `scripts/package-copy-ready.ps1 -PackageDirectory <已验证标准包解压目录>` 生成复制即用包，具体命令见 [README](README.md)。

### 本地数据与手动刷新

- 精灵详情按账号和实例长期保存，最新有效数据覆盖旧文件。点击精灵先显示缓存，在线时更新这一只；批量仓库详情仅刷新勾选的时代。
- 列表刷新保留当前详情；断网、超时及无效响应保留旧内容和时间。
- 图片按种族、外观或图标 ID 使用稳定路径。已有有效图片只读本地，缺图可单独下载；更换插件版本不触发整批重下。
- 设置新增缓存统计、选择性清理、备份恢复和目录迁移。目录选择在升级后沿用；缓存不会自动清理或逐次堆积历史版本。
- 仅点击“检查数据更新”检查官方公共资料。工具依赖准备一次后本地复用，没有后台定时查询。

### 战斗力与培养需求

- 分清官方极限、已装备战力、本地持有可达战力及至高目标。本宠背包中未装备的可用红星参与计算，不只依据官方返回总数。
- “精灵分析”直接显示元魂、源兽、星轮、普通红星和万变红星的具体缺口，不再堆叠词条说明及逐槽分析。
- 对应元魂、源兽和精华显示“需要 / 已有 / 还差”。顶部“刷新材料背包”手动读取普通材料和空闲源兽仓库，不把已装备源兽当作材料库存。
- 时代优先取官方种族 `sign`，皮肤名不会导致错误分类。详情、战力分项、培养缺口和商店资格共用同一套时代开关：该时代没有的系统不显示也不计算。潜能、源兽装备、传说石按官方名称展示。精灵详细的战斗力只保留官方当前/极限、持有可达/至高和培养状态，分项在精灵分析里查看。星轮突破、突破后星灵及其战力仅适用于灵初精灵。
- 列表只在本地持有可达战力与至高目标均已知且达到目标时追加“（至高）”。低于目标或数据未知时不追加后缀，官方极限值不参与该判定。
- 商店与资产复用同一份需求事实，区分真正缺少、已有待装备和需要升级。单只详情更新后刷新对应分析，全账号资产汇总仍由用户手动重算。
- 资产新增独立的“统计本地红星”，分别统计已装备和本宠背包中的普通红星、万变红星；只覆盖已缓存的详情，不联网、不写缓存。

### 官方公共资料与活动兑换

精灵字典、属性和职业名称、星神规则、元魂材料、动态源兽计划、星轮与精华、兑换目录、日常活动和图片资源纳入手动公共更新。详情、商店与资产共用动态源兽计划。新增同结构配置可更新；未知结构保留旧数据并报告原因。

活动兑换通过官方注册表、HUD、近期发布与显式链接发现，处理紧急新增、暂停和 `@sb` 精确进化范围。2026-09-13 的当前公开资源快照为 **8 个目录、72 个指定精灵项目**：

- 全部 72 项有静态限次额度，69 项费用固定，3 项有动态价格条件。
- 商店“刷新兑换次数”读取 7 个目录、68 项的账号次数、活动币及价格条件，并查询标准货币与道具余额。
- 圣冕秘阁 4 项已提供费用和总限 1 次；当前次数读取所需的宿主 `ci` 参数尚未接入。
- 独立活动观察写入当前账号的 `activity-exchanges.json`，按来源和查询范围隔离。V 币、回归币不混用公共物品编号。
- “只读观察”和“上次”数据分别显示；读取成功不自动证明限次周期有效。当前快照不保证覆盖全部历史活动或未来新规则。

商店和商品切换复用已准备的事实与规则，匹配分批处理，避免在界面线程同步扫描、重算全仓库。普通切换不会触发次数或材料网络请求。

### 兼容、构建与升级

源码面向 Windows x64、MSVC 和 Qt 6.6.3。运行时检查宿主 Qt ABI、必要符号和客户端接口，不再限定氪奇 1.1.3/1.1.4 的文件名、哈希或地址；未识别的接口仍需适配。

发行结构采用稳定入口和配对的加载器/DLL 版本目录。部署、更新、回退保留原版客户端与个人数据。设置中选择的数据根独立于插件版本，升级继续复用。具体构建和部署命令见 [README](README.md)。

### 验证范围

此前一份开发源码构建通过了 14 项定向回归，涵盖公共更新、目录、协议、活动观察、材料缓存及相关界面与分析路径。这是对应开发构建的记录，不是对所有功能、所有客户端版本或本次 GitHub 源码整理的完整验收声明。真实客户端交互继续由用户手动验证，发行包的测试报告绑定其具体源码与二进制。

### 相关说明

- [本地缓存与迁移](docs/local-cache-and-manual-updates.md)
- [公共数据更新](docs/manual-public-data-updater.md)
- [战力组成与至高判定](docs/pet-power-composition.md)
- [分析刷新机制](docs/pet-analysis-lifecycle.md)
- [源兽材料背包](docs/source-beast-inventory-protocol.md)
- [活动发现](docs/activity-exchange-public-data.md)与[次数、费用观察](docs/activity-exchange-observations.md)
- [客户端自适配](docs/compatibility-adaptive.md)

历史版本记录可在 Git 历史中查看。旧的单 DLL pending 更新及安装流程已被当前配对版本结构替代，不应继续作为操作说明。
