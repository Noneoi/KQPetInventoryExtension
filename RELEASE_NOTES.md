# KQPetInventoryExtension

这是面向原版氪奇登录器的非官方精灵背包/仓库扩展。扩展通过独立启动器加载，不覆盖、不重打包、不修改原版 `KQPro*.exe`。

## v1.3.1 资产分析刷新语义与性能优化

- 增加账号、实例 ID 与所在区域组成的 `InventorySignature`；60 秒列表同步仅在成员或区域变化时提示重新分析。
- 保留每个账号最后一次有效分析，详情回包只以 O(1) 方式写入去重的 `dirtyPetIds`，不再清空旧结果或重建诊断表。
- 拆分列表数量、成员、详情、商店、日常与账号信号；日常概要轻量更新，商店变化只影响商店适用性状态。
- 手动重新计算后清空脏实例和过期状态；历史快照仍只由明确的用户操作写入。
- 将资产计算、账号分析状态、快照存储、快照比较和设置从 Controller 拆开；诊断与快照列表迁移到 Model/View，后台详情通知不重建表格。
- 分析结果增加 `analysisVersion=1`，新快照使用 `schema=2` 的 `summary + pets` 结构，并兼容读取旧 `schema=1`；未知未来 schema 会被安全忽略。
- 新增 1000/2000 精灵全量分析、详情去重、筛选排序和快照生成性能回归。
- 将 KQPro V1.1.3 的 RVA、签名、Qt 与 QCefView 信息迁入 `TargetProfileRegistry`；未知版本 fail closed。
- 自研 InlineHook 收紧为仅允许 Profile 明确标记的精确无重定位前导；不再允许未验证前导进入 trampoline。后续若支持更多前导，应接入 MinHook 等成熟 relocation 实现。
- 停用会覆盖游戏内挑战冷却的竞技场 `16_24_A` 主动查询，并保证排位赛 `16_6_0` 也绝不由日常概要发送；经典/传奇挑战次数改为在游戏自身打开竞技场时被动读取。
- 删除两个无用的精灵公园次数项，改为接入缤纷树、源兽之门、全民斗技和 2026 新版农场的只读次数；灵骑破封因只有执行协议而保持未读取，避免误操作。
- 修正联盟商店个人贡献币：从联盟概要 `lCToken` 被动更新，不再把通用材料接口未返回的 `134:1` 显示成真实的 0。
- 兑换商店页签只显示商店名称，当前账号拥有的商店资源移到页签上方独立资源栏。
- 兑换商店首次打开改为合并重建，适用个体数量使用一次线性索引，精灵图片目录与大字典延迟到真正查看详情时初始化。

## v1.3.0 资产分析

- 增加统一“资产分析”窗口，集中提供账号资产总览、养成诊断中心和历史快照，不新增服务器协议。
- 账号总览复用当前账号缓存，显示背包、普通仓库、精英仓库、培养完成情况、详情缺失、商店可提升以及日常/周常概要；指标可进入对应诊断列表或现有商店、日常窗口。
- 养成诊断显示当前战斗力、普通极限、最高战斗力、完成度，以及红星、星轮、神源兽、元魂等主要缺口；支持按缺口和商店适用性筛选，双击可在现有精灵窗口定位实例。
- 历史快照按账号、按日期使用 `QSaveFile` 原子保存轻量指标，支持相邻快照变化和单实例历史，不保存完整协议内容。
- 完整资产分析改为用户点击“刷新资产分析”后执行；详情、商店和日常缓存变化只标记分析过期，不在后台反复计算或重建列表。
- 60 秒列表同步继续自动更新精灵总数及三个位置数量。这一路径只进行实例去重和线性计数，不执行培养分析；历史快照可在手动刷新分析时同步更新。
- 新增资产分析 Core Smoke Test 和离屏 UI Preview Test，并纳入 Windows CI 与本地构建脚本。

## v1.2.0 稳定化

- 版本号统一由根 `CMakeLists.txt` 生成，并贯通 DLL 导出、窗口标题和诊断信息；同时记录 Git commit、UTC 构建时间、目标架构和 Qt 版本。
- 启动前新增 fail-closed 兼容性检查：验证 KQPro 身份、x64、Qt6Core/Qt6Widgets 6.6.3、QCefView symbol 和三个协议入口；任一关键项失败时不安装 Hook、不发协议、不创建扩展业务窗口。
- 新增 `KQPetData\logs\latest.log` 和历史日志、账号脱敏、请求/超时/拒绝/缓存/移动/商店/日常关键事件，以及界面“复制诊断”。
- 新增 Windows x64 GitHub Actions：Qt 6.6.3、Release 构建和完整 CTest。
- 新增脱敏真实协议 fixture 回归，并保留 synthetic 边界样本覆盖缺字段、换皮肤和部分请求失败。
- 将详情分析、语义 ViewModel 与 HTML 渲染从 `PetWindow` 分离；内部 CMake target 整理为 `KQPetCore`、`KQPetBridge`、`KQPetUi`，测试和预览不再重复编译同一批业务源文件。
- 普通/精英仓库开始迁移到 Qt Model/View，使用 `PetTableModel` 和 `PetFilterProxyModel` 处理显示、搜索、筛选、排序及单行详情更新；背包分页行为暂时保持不变。

## 主要功能

- 背包、普通仓库、精英仓库分区显示，背包每页最多 12 只，仓库使用滚动列表。
- 账号与实例 ID 双重隔离的本地缓存；仓库概览战斗力只读取当前实例的有效详情缓存。
- 60 秒自动刷新列表，以及可暂停、继续、取消、显示进度的单并发仓库详情刷新队列。
- 点击仓库精灵时先显示缓存，再把该实例插入最高优先级后台刷新队列。
- 支持名称、原名、皮肤名、实例 ID、种族 ID、连续汉字及连续拼音首字母查询，并对命中字符标红。
- 支持战斗力、极限战斗力、图鉴序列、获得时间排序，以及属性、职业、时代筛选。
- 详情显示名称/原名、属性、职业、时代、战斗力、天赋、元魂、神源兽、天迹星轮、星神、召唤/携带/神使关系和养成差距分析。
- 天赋区显示当前/满天赋战斗力；专属星灵显示点亮所需专属精华。
- 星神固定万变独立显示，其余栏位按进攻、防御、功能性归类并使用对应颜色样式。
- 图片与详情落盘缓存；换皮肤但实例 ID 不变时仍识别为同一只精灵。
- 刷新、超时、重试、图片下载和列表更新均采用异步或增量处理，避免阻塞 Qt 主线程。
- 日常/周常/活动概要只手动刷新；玩法次数页接入星轮探险、竞技场和精灵公园的已验证查询协议。
- 移除更新解包与 FUI 预览功能，避免引入不必要的资源下载和解包生命周期风险。

## 安装

1. 关闭正在运行的氪奇登录器。
2. 将发行包内的 `KQPetLauncher.exe` 和 `KQPetInventory.dll` 放入原版氪奇目录，与 `KQPro*.exe` 同级。
3. 以后运行 `KQPetLauncher.exe`。启动器会自动选择目录中版本号最高的 `KQPro*.exe`。

发行包不包含原版氪奇程序、账号缓存、背包/仓库数据、精灵详情缓存或任何个人游戏信息。首次运行后生成的 `KQPetData` 仅保存在用户本机，不应上传到代码仓库或反馈附件。

本项目为非官方学习与扩展项目，与游戏及登录器官方无隶属或授权关系。请在遵守相关用户协议和法律法规的前提下使用。如有侵权，请联系删除。
